# OpenAI Configuration
OPENAI_API_KEY=""

# Whisper Mic Configuration
WHISPER_MODEL=large      # Choose from tiny, base, small, medium, large

# TTS Configuration
TTS_VOICE=english-us    # Adjust based on available voices in pyttsx3
# Assistant Configuration
NOT_DANGEROUS=False  # If True, only execute non-dangerous commands automatically
TALKBACK=True        # If True, use TTS to provide auditory feedback

# TTS Configuration
TTS_VOICE=alloy      # Adjust based on available voices in OpenAI's TTS API
TTS_PROVIDER=openai  # Choose between 'openai' or 'pyttsx3'

# Input/Output Configuration
INPUT_MODE=text     # Choose between 'voice' or 'text'
OUTPUT_MODE=both     # Choose between 'text', 'voice', or 'both'
